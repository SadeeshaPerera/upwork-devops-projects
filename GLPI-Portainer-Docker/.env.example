# Copy this file to `.env` and fill the values before deploying. 
# MariaDB root password
MYSQL_ROOT_PASSWORD=ChangeThisRootPassword

# Database and user for GLPI
MYSQL_DATABASE=glpidb
MYSQL_USER=glpiuser
MYSQL_PASSWORD=ChangeThisDBPassword

# Optional: change host mapping
#HTTP_PORT=8080
